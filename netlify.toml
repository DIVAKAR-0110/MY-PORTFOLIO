# Global configuration
[build.environment]
  SECRETS_SCAN_ENABLED = "false"

# Tell Netlify to use esbuild to bundle firebase-admin properly
[functions]
  node_bundler = "esbuild"
  external_node_modules = ["firebase-admin"]
